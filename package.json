{
  "name": "mouse-batch-util",
  "version": "1.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "flow": "$(npm bin)/flow; test $? -eq 0 -o $? -eq 2",
    "test": "ava **/*.spec.js"
  },
  "dependencies": {
    "opencv": "^6.2.0",
    "robotjs": "^0.6.0"
  },
  "devDependencies": {
    "ava": "^0.19.1",
    "flow-bin": "^0.117.0"
  }
}
